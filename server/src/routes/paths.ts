export enum Path {
  Games = "/games",
  Game = "/games/:game_id",
  Moves = "/moves",
  Players = "/players",
  Player = "/players/:player_id",
  CurrentPlayer = "/players/current",
  SignIn = "/auth/sign-in",
  SignOut = "/auth/sign-out",
  Root = "/",
}
